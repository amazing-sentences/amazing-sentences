#!/usr/bin/env bash

if [ ! -f /var/www/html/.env.production ]; then
    php /var/www/html/artisan env:decrypt --env=production --force
fi

php artisan config:cache

exec /usr/bin/supervisord -c /etc/supervisor/conf.d/supervisord.conf